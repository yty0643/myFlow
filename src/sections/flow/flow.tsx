import React from 'react';
import styles from './flow.module.css';

const Flow = () => {
    return (
        <section className={styles.section} >
            
        </section>
    );
};

export default Flow;