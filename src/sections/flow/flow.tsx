import React from 'react';
import styles from './flow.module.css';

const Flow = () => {
    return (
        <div className={styles.section}>
            
        </div>
    );
};

export default Flow;